# v1.5.2 태그 생성 및 어셈블리 버전 갱신

- 상태: Ready for Verification

## 요구사항
- v1.5.2 태그 생성
- 어셈블리 버전을 v1.5.2 와 동일하게 변경 (`Properties/AssemblyInfo.cs` 파일 갱신)
- 변경 사항을 커밋하고 원격 저장소에 푸시

## 원인 분석 또는 설계
- `.NET 8` WPF 프로젝트의 어셈블리 버전 정보를 담고 있는 `AssemblyInfo.cs` 파일 확인
- `AboutWindow`에서 실제 어셈블리 버전을 가져와 표시하는지 코드 확인
- Git 커밋 및 태그 작업을 수행

## 구현 내용
- `Folderss/Properties/AssemblyInfo.cs` 내의 `AssemblyVersion` 및 `AssemblyFileVersion` 값을 `1.5.2.0`으로 수정
- `AboutWindow`가 정상적으로 어셈블리 버전을 읽어 표시하는 것을 코드 상에서 확인

## 변경 파일
- `Folderss/Properties/AssemblyInfo.cs`

## 검증
- [x] 빌드 성공 확인 (`dotnet build .\Folderss.sln -c Debug`)
- [x] 어셈블리 버전 정보가 `1.5.2.0`으로 반영되었는지 확인
- [x] Git 태그 `v1.5.2` 생성 및 푸시 완료 확인

## 변경 이력
- 2026-06-30: 작업 정의서 생성 (상태: Todo)
- 2026-06-30: 버전 정보 수정 및 로컬 빌드 성공 (상태: Ready for Verification)
