<UserControl x:Class="Folderss.Controls.FavoritesPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="180">
    <Grid Background="{DynamicResource PanelBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Padding="10,8"
                Background="{DynamicResource SurfaceBackground}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,0,0,1">
            <DockPanel>
                <TextBlock Text="즐겨찾기" FontWeight="SemiBold" VerticalAlignment="Center"/>
                <Button DockPanel.Dock="Right"
                        Content="+"
                        ToolTip="현재 활성 폴더 추가"
                        Padding="9,3"
                        Click="AddCurrent_Click"/>
            </DockPanel>
        </Border>

        <ListBox x:Name="FavoritesList"
                 Grid.Row="1"
                 Background="{DynamicResource PanelBackground}"
                 BorderThickness="0"
                 MouseDoubleClick="FavoritesList_MouseDoubleClick">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="3">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="28"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="★"
                                   Foreground="{DynamicResource AccentBrush}"
                                   VerticalAlignment="Center"/>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding Path}"
                                       Foreground="{DynamicResource SecondaryText}"
                                       FontSize="11"
                                       TextTrimming="CharacterEllipsis"
                                       ToolTip="{Binding Path}"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Border Grid.Row="2"
                Padding="5"
                Background="{DynamicResource SurfaceBackground}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="↑" ToolTip="위로 이동" Padding="8,3" Click="MoveUp_Click"/>
                <Button Content="↓" ToolTip="아래로 이동" Padding="8,3" Click="MoveDown_Click"/>
                <Button Content="제거" Padding="9,3" Click="Remove_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
